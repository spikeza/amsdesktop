<Window x:Class="AMSDesktop.UI.Apartment.SelectApartment"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AMSDesktop.UI.Apartment"
        mc:Ignorable="d"
        Style="{StaticResource FontStyle}"
        Title="เลือกอพาร์ตเมนต์" Height="150" Width="400">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" HorizontalAlignment="Center" Margin="0,10,0,0">
            <WrapPanel Margin="0,10,0,0">
                <Label Content="เลือกอพาร์ตเมนต์" Width="120"/>
                <ComboBox Name="cbxApartment" Width="220"/>
            </WrapPanel>
            <WrapPanel HorizontalAlignment="Center" Margin="0,20,0,20">
                <Button x:Name="btnSubmit" Height="30" Width="100" Content="ตกลง" Click="btnSubmit_Click"/>
            </WrapPanel>
        </StackPanel>
    </DockPanel>
</Window>
